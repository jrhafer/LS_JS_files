if (true) {
  let myValue = 20;
}

console.log(myValue);

// Error: the my varaible myValue is not accessible in the
// global scope, where we are attempting to log it. It is 
// declared within a block, thereby giving it block 
// scope.
