console.log(greeting);

var greeting = 'Hello world!';

// undefined
// the greeting variable is hoisted above the console.log 
// method invocation but is not initialized to a value until 
// after the method is invoked. Therefore it returns undefined

