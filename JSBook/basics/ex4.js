console.log('5' + 10);

// '510'

// when we use the + operator, if one of the operators is a 
// string, the other will be coerced into a string. The result is the 
// concatenation of '5' and '10'
// - we call this implicit coercion

